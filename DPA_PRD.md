# Delta PLC AI Generator (DPA)
## وثيقة متطلبات المنتج — الإصدار 2.0

---

| الحقل | القيمة |
|---|---|
| **اسم المشروع** | Delta PLC AI Generator (DPA) |
| **الإصدار** | 2.0 — النسخة المحدّثة للتنفيذ |
| **تاريخ الإصدار** | مايو 2026 |
| **حالة المستند** | معتمد ومغلق — Production Ready |
| **نموذج العمل** | مجاني بالكامل (Free) — مع خطة للتحويل لدفعة واحدة لاحقاً |
| **نموذج الـ API** | BYOK (Bring Your Own Key) مع دليل إعداد مدمج |
| **المحاكي** | مؤجّل — خارج نطاق MVP |

---

## 1. مقدمة ورؤية المنتج

هدف هذا المشروع هو بناء تطبيق سطح مكتب ذكي ومبتكر مخصص لنظام التشغيل Windows، يساعد مهندسي الأتمتة والتحكم الصناعي على توليد أكواد ومخططات التحكم الخاصة بـ Delta PLC (DVP Series) بالإضافة إلى جداول متغيرات شاشات التشغيل HMI عبر الذكاء الاصطناعي.

يحل البرنامج ثلاث مشكلات جوهرية في البيئات الصناعية:

- الوقت الطويل المستغرق في كتابة الأكواد وتصميم المخططات السلمية يدوياً.
- التضارب في عناوين الذاكرة بين الـ PLC والشاشة.
- الأخطاء البشرية في برمجة الآلات الحساسة.

> **القرارات الاستراتيجية الجديدة في v2.0**
>
> ١- نموذج مجاني كامل في مرحلة الإطلاق، مع تصميم يسمح بالتحويل السلس لدفعة واحدة لاحقاً.
> ٢- BYOK مع Wizard إعداد مدمج خطوة بخطوة — صفر تكلفة API على المطوّر.
> ٣- حذف المحاكي التفاعلي من MVP وتركيز الجهد على قيمة التوليد والتحقق النصي.

---

## 2. معمارية النظام والتقنيات

### 2.1 نوع التطبيق

تطبيق سطح مكتب (Desktop Application) يعمل على نظام التشغيل Windows بشكل خفيف وسريع، مصمم للعمل دون حساب سحابي أو تسجيل دخول.

### 2.2 Tech Stack

| الطبقة | التقنية | السبب |
|---|---|---|
| Frontend | React.js + Tailwind CSS | واجهة حديثة وسريعة التطوير |
| Desktop Core | Tauri (Rust) | حجم 10–15 MB، وصول آمن للملفات |
| محرك الويب | Microsoft Edge WebView2 | مدمج في Windows — لا تثبيت إضافي |
| AI Engine | Anthropic / OpenAI API (BYOK) | مرونة الاختيار للمستخدم |
| Ladder Diagram | React Flow / SVG | رسم ديناميكي للمخطط السلمي |
| حفظ البيانات | ملفات محلية `.dpa` | خصوصية وأمان كامل |

### 2.3 نموذج الاتصال والـ API (BYOK)

يعتمد البرنامج نظام BYOK بالكامل — صفر تكلفة API على صاحب المشروع. للتغلب على عقبة الإعداد يُبنى داخل التطبيق دليل إعداد تفاعلي:

- شاشة ترحيب تكتشف عدم وجود مفتاح وتفتح Wizard تلقائياً.
- خطوات مرئية موضّحة بالصور: كيف تسجل في Anthropic أو OpenAI وتحصل على المفتاح.
- رابط مباشر لصفحة توليد المفتاح يفتح في المتصفح بنقرة واحدة.
- حقل إدخال المفتاح مع زر اختبار فوري (Test Connection) يتحقق من صحته قبل المتابعة.

#### المزوّدون المدعومون (M11+)

| المزوّد | النوع | الموديلات | ملاحظات |
|---|---|---|---|
| **OpenAI** | Native | GPT-4o, GPT-4 Turbo, GPT-3.5 Turbo,... | API key عبر `Authorization: Bearer` |
| **Anthropic** | Native | Claude Sonnet 4.6, Claude Haiku 4.5,... | API key عبر `x-api-key` + `anthropic-version` |
| **Google Gemini** (M11.2) | Native | `gemini-2.5-pro`, `gemini-2.5-flash`, `gemini-2.5-flash-lite` + custom | API key عبر `x-goog-api-key` (لا في query string)؛ يستخدم `systemInstruction` بدلاً من system message |
| **Custom Provider** (M11.3) | OpenAI-compatible | حر — يحدده المستخدم | حقلا `Custom Base URL` + `Custom Model Name`. يتطلب توافق `POST /chat/completions` streaming. HTTPS إلزامي عدا `localhost`/`127.0.0.1`. يخضع لنظام **Trust on First Use** (M11.4) — أول استخدام لـ domain جديد يطلب تأكيد المستخدم عبر modal قبل إرسال أي بيانات |

#### Trust on First Use (M11.4) — المزوّدات المخصصة

عند تهيئة Custom Provider لأول مرة بـ domain جديد:

1. الواجهة تعرض modal: "أنت على وشك إرسال بيانات مشروعك إلى `{domain}` — تأكد أنك تثق به".
2. التأكيد يضيف الـ domain إلى `<app_data_dir>/trusted_domains.json` (نص JSON قابل للقراءة البشرية).
3. الإزالة تتطلب موافقة جديدة. الـ cloud metadata endpoint (`169.254.x.x`) مرفوض **دائماً** (حماية SSRF) حتى لو وُجد في القائمة.

### 2.4 إدارة البيانات والخصوصية

- حفظ المشاريع كملفات محلية بامتداد `.dpa` على جهاز المهندس فقط.
- لا يُرسل أي كود أو نص مشروع لأي خادم خارجي باستثناء طلب الـ API المشفر للنموذج المختار.
- المفتاح يُحفظ محلياً ومشفراً باستخدام مخزن Tauri الآمن (OS Keychain).

### 2.5 تصميم قابل للتحويل (Free → Paid)

يُبنى التطبيق من اليوم الأول بحيث يسهل لاحقاً تقسيم الميزات إلى مجانية ومدفوعة دون إعادة هيكلة:

| مجاني دائماً (Free Tier) | مدفوع لاحقاً (Paid — دفعة واحدة) |
|---|---|
| توليد الأكواد ST و LD | تصدير XML لـ ISPSoft |
| جدول I/O والتسميات | تصدير CSV لـ DOPSoft |
| التقرير النصي الذكي | دعم نماذج AI متعددة |
| حفظ وفتح المشاريع `.dpa` | تحديثات مدى الحياة |
| دليل الإعداد BYOK | ميزات مستقبلية (AS Series...) |

---

## 3. المتطلبات الوظيفية والميزات

### 3.1 آلية الإدخال والتفاعل

#### 3.1.1 جدول عناوين المداخل والمخارج (I/O Mapping Table)

لوحة منظمة في القائمة الجانبية يحدد فيها المهندس يدوياً المداخل والمخارج والريلايات قبل البدء:

- أعمدة الجدول: العنوان (X0، Y0، M0...)، النوع (Input/Output/Relay)، التسمية الوصفية، القيمة الافتراضية.
- دعم التوسعة: الجدول مفتوح لإضافة عناوين تتجاوز الحد الافتراضي للموديل مع تنبيه أصفر.
- التسميات تُسحب تلقائياً كتعليقات فوق الرموز في الكود النهائي.

#### 3.1.2 صندوق الوصف الأساسي

خانة مخصصة يكتب فيها المهندس أو يسجل صوته لشرح آلية عمل النظام:

- دعم الإدخال باللغة الإنجليزية الكاملة أو المزيج الهجين (عربي/عامية + مصطلحات هندسية إنجليزية).
- تسجيل صوتي مدمج عبر المايك مع تحويل تلقائي للنص.

#### 3.1.3 نافذة التعديل المستمر (Chat Panel)

نافذة جانبية ذكية للتعديلات التراكمية على الكود:

- يرسل البرنامج جدول I/O في كل طلب تعديل كـ Context ثابت لا يُمسّ.
- الذكاء الاصطناعي يُحدّث الأجزاء المطلوبة فقط دون تدمير الهيكل البرمجي الأصلي.
- سجل المحادثة يُحفظ ضمن ملف المشروع `.dpa`.

---

### 3.2 المخرجات والتصدير

#### 3.2.1 المخرجات الثنائية للـ PLC

يولّد البرنامج نوعين من الأكواد في نفس الوقت ويعرضهما في شاشة مقسمة:

| نوع المخرج | الوصف | العرض |
|---|---|---|
| Structured Text (ST) | كود نصي مهيكل | Syntax Highlighting ملون |
| Ladder Diagram (LD) | مخطط سلمي رسومي | React Flow / SVG تفاعلي |
| Instruction List (IL) | قائمة التعليمات النصية | قابلة للنسخ مباشرة لـ WPLSoft |

#### 3.2.2 مخرجات شاشة التشغيل (HMI Tags)

- استنتاج تلقائي لعناصر الشاشة من وصف المستخدم (أزرار، لمبات بيان، تنبيهات).
- حجز ريلايات داخلية (M) تلقائياً وعرضها في جدول HMI Tag منفصل ومنظم.
- منع تضارب العناوين بين PLC و HMI تلقائياً.

#### 3.2.3 آليات التصدير

| صيغة التصدير | البرنامج المستهدف | الآلية |
|---|---|---|
| XML | ISPSoft | استيراد مباشر لأكواد PLC |
| CSV | DOPSoft | استيراد جدول Tags لشاشات HMI |
| نسخ نصي | WPLSoft | لصق Instruction List مباشرة |

---

### 3.3 السلامة والتحقق

#### 3.3.1 التقرير النصي الذكي (AI Review)

- تقرير جانبي يكتبه الذكاء الاصطناعي يشرح بنقاط مبسطة ماذا سيفعل الكود في الواقع.
- يظهر بجانب الكود المولّد ليراجعه المهندس قبل التشغيل الحقيقي.
- يشمل: الحالات الاستثنائية المحتملة، المؤقتات المستخدمة وقيمها، تسلسل التشغيل والإيقاف.

#### 3.3.2 نظام التنبيه الذكي للموديلات

- تنبيه أصفر تحذيري عند تجاوز عدد المداخل/المخارج الافتراضية للموديل المحدد.
- مثال: *"تنبيه: DVP-14SS2 يحتوي على 8 مداخل افتراضياً — العناوين المضافة تتطلب كرت توسعة."*

#### 3.3.3 معالجة التناقضات (Conflict Resolution)

- عند رصد تعارض منطقي في وصف المستخدم: توقف فوري عن التوليد.
- تلوين الجمل المتضاربة بالأحمر وفتح نافذة المحادثة لاستجواب المهندس.
- منع أي تخمين من الذكاء الاصطناعي في بيئات الآلات الخطرة.

---

## 4. النطاق التقني والتوافق

### 4.1 العائلة المستهدفة

> **Delta DVP Series — فقط في MVP**
>
> الدعم الكامل لعائلة Delta DVP Series بجميع فئاتها: `DVP-SS2`, `DVP-SE`, `DVP-SX2`, `DVP-SV2`.
> توافق 100% مع WPLSoft و ISPSoft.
> دعم سلاسل أخرى (AS Series أو شركات أخرى) خارج النطاق حالياً ومخطط للإصدارات المستقبلية.

### 4.2 المنطق المدعوم (الإصدار الأول)

يركز البرنامج على الإشارات الرقمية (Digital Logic) فقط:

| مدعوم ✓ | مدعوم جزئياً ⚠ | خارج النطاق ✗ |
|---|---|---|
| بوابات المنطق الأساسية | كروت التوسعة (تنبيه فقط) | الإشارات التماثلية Analog |
| SET / RST | — | مسجلات البيانات D المعقدة |
| المؤقتات T (جميع الأنواع) | — | شبكات الاتصال PLC-PLC |
| العدادات C | — | Motion Control |
| الريلايات الداخلية M | — | — |

### 4.3 معالجة المؤقتات والعدادات

- توليد تلقائي إذا ذُكرت في النص مع إضافتها للجدول بصيغة الثوابت K المناسبة (مثل `K50` لخمس ثوانٍ).
- أو حجزها مسبقاً في جدول العنونة بقيم محددة يلتزم بها البرنامج تماماً.

### 4.4 نظام التعليقات (Symbolic Annotations)

- سحب التسميات الوصفية من جدول العنونة وزرعها كتعليقات فوق الرموز البرمجية المقابلة.
- ضمان نظافة وترتيب الكود النهائي دون ازدحام بصري.

---

## 5. المتطلبات غير الوظيفية

| المعيار | المتطلب | الهدف |
|---|---|---|
| الحجم | ملف تثبيت ≤ 20 MB | خفيف وسريع التنزيل والتثبيت |
| الأداء | استجابة الواجهة < 200ms | تجربة مستخدم سلسة |
| الخصوصية | لا بيانات على خوادم خارجية | أمان كود المصانع الحساس |
| التوافق | Windows 10/11 | 64-bit فقط |
| الاعتمادية | يعمل بدون إنترنت (جزئياً) | الواجهة والملفات تعمل offline |

---

## 6. الشاشات الرئيسية

### 6.1 شاشة الترحيب (Welcome Screen)

- زر "مشروع جديد" — يفتح Workspace فارغاً.
- زر "فتح ملف مشروع `.dpa`" — يستعيد مشروع محفوظ.
- قائمة "المشاريع الأخيرة" — وصول سريع لآخر 10 مشاريع.
- اكتشاف تلقائي لغياب API Key وفتح دليل الإعداد مباشرة.

### 6.2 لوحة العمل الرئيسية (Main Workspace)

Single Page Workspace مقسم لأربعة أقسام رئيسية:

| القسم | المحتوى |
|---|---|
| الشريط الجانبي الأيسر | جدول I/O + حجز المؤقتات والعدادات + تسميات HMI |
| المنطقة المركزية العلوية | صندوق الوصف + زر التوليد |
| المنطقة المركزية السفلية | تبويبات: ST \| LD \| IL مع زر Maximize للـ LD |
| الشريط الجانبي الأيمن | التقرير النصي الذكي + Chat Panel للتعديلات |

### 6.3 لوحة الإعدادات (Settings Panel)

- حقل إدخال API Key محمي (مخفي عند الكتابة) مع زر اختبار الاتصال.
- قائمة منسدلة لاختيار مزوّد الخدمة: Anthropic / OpenAI.
- قائمة منسدلة لاختيار النموذج المفضل.
- دليل الإعداد BYOK المدمج (Wizard تفاعلي).

---

## 7. معالجة الأخطاء والاستثناءات

### 7.1 أخطاء الاتصال والـ API

| نوع الخطأ | الإجراء | تجربة المستخدم |
|---|---|---|
| انقطاع الإنترنت | حفظ مسودة محلية تلقائياً | إشعار غير مُجمِّد للواجهة |
| مفتاح API خاطئ | قراءة رمز الخطأ وتفسيره | توجيه للإعدادات بخطوات واضحة |
| رصيد API منتهٍ | رسالة محددة بالسبب | رابط مباشر لصفحة إعادة الشحن |
| تعارض منطقي في الكود | توقف توليد فوري | تلوين الجمل المتعارضة بالأحمر |

---

## 8. المخاطر والتحديات وحلولها

| # | التحدي | المخاطرة | الحل المتخذ |
|---|---|---|---|
| 1 | هلوسة الذكاء الاصطناعي | اختراع تعليمات غير موجودة في Delta DVP | Prompt Engineering صارم مع Cheatsheets مرجعية لـ DVP فقط |
| 2 | تخريب الكود عند التعديل | الـ AI يعيد كتابة العناوين المحجوزة | Context Anchoring: إرسال جدول I/O كنص ثابت مع كل طلب |
| 3 | رسم Ladder Diagram | تحويل النص لرسوم كهربائية منسقة | خوارزمية مخصصة تقرأ IL وتحوّلها لـ SVG/React Flow ديناميكياً |
| 4 | صيغ دلتا المغلقة | عدم الكتابة المباشرة لـ WPLSoft | XML لـ ISPSoft + CSV لـ DOPSoft — صيغ مفتوحة مقبولة رسمياً |
| 5 | عقبة BYOK الإعداد | المهندس يتخلى عند طلب API Key | Wizard مدمج خطوة بخطوة مع صور وروابط مباشرة واختبار فوري |

---

## 9. خارج نطاق MVP

العناصر المؤجّلة للإصدارات المستقبلية:

- المحاكي التفاعلي المرئي — مؤجّل بالكامل لـ v2.x
- الإشارات التماثلية Analog ومسجلات D المعقدة
- دعم Delta AS Series وسلاسل أخرى
- دعم شركات أخرى (Siemens, Mitsubishi...)
- نموذج الاشتراك الشهري — التحويل لدفعة واحدة يأتي بعد جمع بيانات الاستخدام

---

## 10. مقاييس النجاح

| المقياس | الهدف (شهر 3) | الهدف (شهر 6) |
|---|---|---|
| عدد التنزيلات | 500+ | 2,000+ |
| معدل الإكمال لأول كود مولّد | ≥ 60% | ≥ 75% |
| معدل التخلي عند إعداد API Key | < 40% | < 25% |
| التقييم العام (Feedback) | ≥ 4/5 | ≥ 4.5/5 |
| المشاريع المحفوظة لكل مستخدم نشط | ≥ 3 | ≥ 7 |

---

*Delta PLC AI Generator — PRD v2.0 • مايو 2026*
